import { serverApiFetch, ServerApiError } from '../../lib/api/server-api-client';
import { PageHeader, StatusPill } from '@repo/ui/workspace';
type Plan = { title: string; status: string; goals: Array<{ title: string; status: string }>; exercises: Array<{ name: string }> } | null;
export default async function PatientDashboard() {
  let me: { patient: { firstName: string } }; let plan: Plan;
  try { [me, plan] = await Promise.all([serverApiFetch<typeof me>('/api/v1/patient-portal/me'), serverApiFetch<Plan>('/api/v1/patient-portal/plan')]); } catch (e) { if (e instanceof ServerApiError) return <p role="alert">Не вдалося завантажити дані порталу. Спробуйте ще раз.</p>; throw e; }
  return <div className="space-y-8"><PageHeader eyebrow="Ваш прогрес" title={`Вітаємо, ${me.patient.firstName}`} description="Ваш особистий простір реабілітації." /><section className="ui-filter-bar"><p className="rc-kicker">Сьогодні</p><h2 className="mt-2 text-xl font-semibold tracking-tight">Маленький крок — великий результат</h2><div className="mt-5 flex flex-wrap gap-3"><a className="rc-btn rc-btn-primary" href="/patient/daily-report">Заповнити щоденний звіт</a><a className="rc-btn rc-btn-secondary" href="/patient/exercises">Вправи на сьогодні</a></div></section>{plan ? <section className="ui-filter-bar"><div className="flex flex-wrap items-center justify-between gap-3"><div><p className="rc-kicker">Ваш план</p><h2 className="mt-2 text-xl font-semibold tracking-tight">{plan.title}</h2></div><StatusPill tone={plan.status === 'PAUSED' ? 'warning' : 'success'}>{plan.status === 'PAUSED' ? 'Призупинено' : 'Активний'}</StatusPill></div><p className="mt-4 border-t border-border pt-4 text-sm text-text-secondary">Цілей: {plan.goals.length} · Призначених вправ: {plan.exercises.length}</p></section> : <section className="ui-filter-bar"><h2 className="text-base font-semibold">Поточний план</h2><p className="mt-2 text-text-secondary">Активного плану поки немає.</p></section>}</div>;
}
